# Production environment overrides for gRINN Web Service
# Copy to .env.production on your production server and customize

# =============================================================================
# PUBLIC URL CONFIGURATION (REQUIRED FOR PRODUCTION)
# =============================================================================
# Your public domain/IP
PUBLIC_HOST=grinn.bio-cloud.site

# Backend API URL (through nginx proxy)
BACKEND_PUBLIC_URL=https://grinn.bio-cloud.site:8081/api

# Dashboard public host (for direct port access fallback - usually not used in production)
DASHBOARD_PUBLIC_HOST=grinn.bio-cloud.site

# Dashboard URL template (proxied through nginx/backend)
# Use {job_id} placeholder - backend proxies requests to dashboard containers
DASHBOARD_PUBLIC_URL_TEMPLATE=https://grinn.bio-cloud.site:8081/api/dashboard/{job_id}

# =============================================================================
# DATABASE (use PostgreSQL in production)
# =============================================================================
DATABASE_URL=postgresql://grinn_user:${POSTGRES_PASSWORD}@postgres:5432/grinn_web
POSTGRES_PASSWORD=your-secure-password-here

# =============================================================================
# SECURITY (CHANGE THESE!)
# =============================================================================
SECRET_KEY=generate-a-secure-random-string-here
JWT_SECRET_KEY=generate-another-secure-random-string-here
WORKER_REGISTRATION_TOKEN=generate-a-worker-token-here

# =============================================================================
# STORAGE
# =============================================================================
STORAGE_PATH=/path/to/your/grinn-jobs

# =============================================================================
# OTHER SETTINGS (adjust as needed)
# =============================================================================
LOG_LEVEL=INFO
DEBUG=false
FLASK_ENV=production
DASH_ENV=production
